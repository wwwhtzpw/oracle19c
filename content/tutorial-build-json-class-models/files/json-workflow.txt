The illustration shows th workflow for the JSON Data Classification model.

On the left a data source icon is labeled ODMR_SALES_JSON_DATA, to the right is an icon labeled JSON query. An arrows extends from the data source icon to the JSON query icon indicating a connection.

To the right of the JSON query icon is a table icon labeled Table for Performance. An arrow extends from the JSON Query icon tot he table icon, indicating a connection.

To the right of the table icon is an icon indicating a classification node. An arrow extends from the tabel icon to the classification node, indicating a connection.