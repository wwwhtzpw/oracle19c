The illustration shows the New / Select Database Connection dialog. The dialog shows the dialog name as PDB1-DMUSER, the Database Type is Oracle.

Below, the User Info tab is selected. On this tab, the Authentication type is selected as Default. The Username field contains DMUSER in upper case characters. The role type is selected as a default.
Below, the Connection type is selected as basic, and the details tab is selected.
The hostname field is localhost, the port number is set to 1521, and the service name is set to orclpdb1.example.com.
The Test button is highlighted, and the status field shows Status: Success.