The illustration shows the Edit Prediction Query Node window.

The Predictions tab is selected.

Case ID is set to CUST_ID.

A table labeled Targets with three columns is displayed. The columns are labeled Name, Data Types and Mining Type. The first row contains the data values AFFINITY_CARD, NUMBER and the Mining Type is an icon labeled Numerical. The third column is selected to show setting the column type to Categorical. 

An arrow extends from the selected Categorical type to a dialog labeled Warning.

The text of this dialog states: Prediction putputs for this target will be replaced with the defaults. Would you like to proceed. 

The Yes button of the dialog is highighted.