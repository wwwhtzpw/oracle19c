The illustration shows how to remove prediction outputs.

In the Prediction Outputs section of the Edit Prediction Query Node window, three Production Details are selected.

In the upper right, the red X representing delete is highlighted.

At the bottom, the OK button is highlighted.

