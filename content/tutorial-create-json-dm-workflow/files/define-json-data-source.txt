The illustration shows how a data source is dragged from the Components window to the workflow.

In the Define Data Source window, DMUSER_ODMR_SALES_JSON_DATA is selected.

An arrow extends from the selected table to a table labeled JSON_DATA.