The illustration shows how to add a partition column.

In the Edit Clustering Query Node, the Partition tab is selected. The Add Partition Column icon (a green plus sign) is highlighted, and the an arrow extends to the Add Partition Column dialog.

In the dialog, Country_name is one of the selected attributes. The OK button in the dialog in highlighted.