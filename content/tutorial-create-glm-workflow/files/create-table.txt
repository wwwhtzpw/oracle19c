The illustration shows the Edit Create Table or View Mode window.

At the top of the wizard, the name field contains OUTPUT_1_4.

The Table radio button is selected for Type.

A checkbox Auto Inout Columns Selection is highlighted and unselected.

A table labeled Data contains 5 columns, labeled Target Name, Target Type, Key, Index and Alias.

A row that contains CUST_ID has a check mark on the index column.

Below the table, the OK button is highlighted.