The illustration shows the connections between the data source, explore data, graph and class build icons.
On the left, an icon representing a data source is labeled INSUR_CUST_LTV_SAMPLE. An arrow extends from this icon to an icon labeled Explore data. Another arrow extends from the data source to an icon labeled Class Build. A third arrow extends from the data source to an icon labeled Graph.