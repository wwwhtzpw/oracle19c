This illustration shows the workflow extended to include a data table to persist the results.

On the left, an icon representing a data source is labeled INSUR_CUST_LTV_SAMPLE. An arrow extends from this icon to an icon labeled Explore data.  An arrow extends from the data source to an icon labeled Graph. An arrow extends from the data source to an icon labeled Class Build.

An arrow extends from the class build icon to an icon labeled Apply Model. Above the apply model icon, there is an data source icon labeled INSUR_CUST_LTV_APPLY. An arrow extends from this data source to the Apply Model icon.

From the apply model icon, an arrow extends to a data table icon labeled DT_PREDICTIONS.